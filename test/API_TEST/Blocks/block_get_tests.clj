(ns clojurena.API-TEST.Blocks.block-get-tests
  (:require [clojure.test :refer :all]
            [clj-http.client :as client]
            [clojurena.API.Blocks.block-get-request :refer :all]))

(deftest get-block-by-id
  (testing "Ability to retrieve individual blocks"
    (is =)))
