(ns clojurena.core-test
  (:require [clojure.test :refer :all]
            [block-delete-tests :as block-del]
            [block-get-tests :as block-get]
            [block-post-tests :as block-post]
            [block-put-tests :as block-put]
            [channel-delete-tests :as channel-del]
            [channel-get-tests :as channel-get]
            [channel-post-tests :as channel-post]
            [channel-put-tests :as channel-puts]
            [search-get-tests :as search-get]
            [user-get-tests :as user-get]))
            
            
            
            

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
